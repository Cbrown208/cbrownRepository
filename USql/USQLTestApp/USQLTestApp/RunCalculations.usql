// Values passed in by the pipeline
DECLARE EXTERNAL @PatientSex = "M";

USE ServiceCategory;

// Fancy Calculations go here 
@output = SELECT * FROM dbo.BillMast WHERE PatientSex == @PatientSex;

OUTPUT @output 
    TO "/ServiceCategory/52060/Output/CalculationResults.csv"
    USING Outputters.Csv(outputHeader:true, quoting: false);